{
  "app": "ay-app",
  "platform": "next",
  "port": 3000,
  "build": {
    "location": "iran"
  },
  "cron": [
    {
      "schedule": "0 4 * * *",
      "command": "curl -sf -X GET http://localhost:3000/api/cron -H \"Authorization: Bearer $CRON_SECRET\" --max-time 120 || true"
    }
  ]
}
